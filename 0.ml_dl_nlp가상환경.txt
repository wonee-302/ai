※ 가상환경 만들기(ml-dl-nlp / llm)

(현재 존재하는 가상환경 목록 확인) conda env list
                                             conda info --envs
(가상환경 생성;-n은 --name) conda create -n ml-dl-nlp python=3.10
(생성한 가상환경으로 전환) conda activate ml-dl-nlp
(pip 최신버전 업그레이드) python -m pip install --upgrade pip
pip install jupyter numpy==1.23.5 pandas==1.5.3 matplotlib==3.7.0 seaborn==0.12.2 scikit-learn==1.2.1
pip install tensorflow==2.10

(가상환경을 jupyter에서 선택가능한 커널로 등록)
conda install ipykernel
python -m ipykernel install --user --name ml-dl-nlp --display-name "ml-dl-nlp(ipykernel)"

(가상환경을 jupyter커널 등록확인) jupyter kernelspec list

(가상환경을 jupyter 커널 목록에서 삭제하기) jupyter kernelspec remove "ml-dl-nlp(ipykernel)"
(가상환경 삭제) conda env remove --name ml-dl-nlp

※ 딥러닝에서 엑셀데이터 불러오기
pip install openpyxl (3.1.5)

※ mnist 중간에 설치한 것
pip install opencv-python==4.8.1.78

※ 자연어처리에 설치한 것
pip install nltk==3.7        # --no-warn-script-location # 영어권 자연어처리
pip install konlpy # (0.6.0)한글 자연어 전처리(java필요)
pip install python-mecab-ko #(1.3.7)한글자연어 전처리(C++필요)
pip install wordcloud # (1.9.6)
pip install gensim # (4.4.0) 워드임베딩(단어간 거리 계산)
pip install  python-dotenv # .env읽어들이기(강추)
pip install python-decouple # .env읽어들이기(주피터환경에서 안 될 수도 있음)

pip install apyori #(1.1.2)

pip install statsmodels==0.13.5 # 통계 패키지중 시계열 
pip install prophet #(1.4.0) facebook에서 개발한 시계열 분석, 예측
pip install plotly(선택, prophet에서 경고)















