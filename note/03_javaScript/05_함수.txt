1. 함수:자료형
   익명 함수 :이름없는 함수
   선언적 함수: 이름없는 함수

2. 화살표 함수:익명 함수를 짧게 쓴 형태
 매개변수 개수에 따라 (), x, (x, y)처럼 작성

3. 함수는 매개변수로 값을 받고 return으로 결과를 돌려줌
  매개변수 값이 부족하면 undefined 많으면 무시

4. arguments 사용시 전달받은 모든 값을 처리
  내부 함수:  함수 안의 함수
 콜백 함수: 다른 함수에 전달된 함수

5. setTimeout()은 일정 시간 후 한 번
setInterval()은 일정 시간마다 반복 실행
clearTimeout(), clearInterval()로 중지






